const validator = require("validator");
const chalk = require("chalk");

// console.log(validator.isEmail("gema@gema.com"));
console.log(validator.isNumeric("0700af79"));
const pesan = chalk`lorem ipsum asf {bgRed.yellow.bold ags} adg ad {bgRed.black dah adh} adgadgad gadgh`;
console.log(pesan);
